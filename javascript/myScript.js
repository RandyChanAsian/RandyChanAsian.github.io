for(x=1; x<9; x++) {
  var ans = Math.pow(2,x);
  document.write(ans + "<br/>");
}
function start(){
	var name = readLine("What do you use? Apple or Samsung? ");
	println("Name is: " + name);
	
	var age = readInt("What is your age? ");
	println("Age is: " + age);
}
